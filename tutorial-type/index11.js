// interface
let 네모 = { color: 'red', width: 100 };
let 학생 = { name: 'kim' };
let 선생 = { name: '선생', age: 20 };
// type VS interface
// interface는 중복선언 가능
// type은 중복선언 불가능
// 외부 라이브러리같은 경우 interface를 많이 사용한다.
// &기호 쓸때 중복속성이 발생하면 미리 에러가 안나옴
