// 타입을 파라미터로 입력하는 Generic
// 타입 파라미터 입력하는 함수 (Generic)
function 함수(x) {
    return x[0];
}
let a = 함수([4, 2]);
//타입파라미터 제한두기
function 함수2(a) {
    return a - 1;
}
let y = 함수2(100);
function 함수(g) {
    return x.length;
}
